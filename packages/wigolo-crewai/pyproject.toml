[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "wigolo-crewai"
version = "0.2.0"
description = "CrewAI integration for wigolo — local-first web intelligence tools"
readme = "README.md"
license = "AGPL-3.0-only"
requires-python = ">=3.10"
authors = [{ name = "KnockOutEZ" }]
keywords = ["crewai", "mcp", "web-search", "agent-tools", "research"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: Other/Proprietary License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Libraries",
]
dependencies = [
    "wigolo>=0.1.0",
    "pydantic>=2.0.0",
]

[project.optional-dependencies]
crewai = [
    "crewai>=0.70.0",
]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.24",
    "pytest-mock>=3.14",
]

[project.urls]
Homepage = "https://github.com/KnockOutEZ/wigolo"
Repository = "https://github.com/KnockOutEZ/wigolo/tree/main/packages/wigolo-crewai"
Issues = "https://github.com/KnockOutEZ/wigolo/issues"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
